[project]
name = "304-multiplayer-backend"
version = "0.1.0"
description = "FastAPI backend for the 304 multiplayer / vs-bots flow (local dev only; not part of the public site)."
requires-python = ">=3.11"
license = {text = "MIT"}

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[tool.pytest.ini_options]
testpaths = ["multiplayer/tests"]
